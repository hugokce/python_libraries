notlar bu eğitimden alınmıştır

https://www.udemy.com/course/deep-learning-prerequisites-the-numpy-stack-in-python/learn/lecture/19643200?start=0#overview

6 saat

https://lazyprogrammer.me/numpy

colabtan çalışacağız

colab dosyalarını numpy sayfasında paylaştı indirildi
numpy, pandas, matplotlib ve scipy

https://deeplearningcourses.com/course_order
https://lazyprogrammer.me/cs-degree

conda install pip
pip install tensorflow
pip install keras
pip install https://cntk.ai/...whl
pip install koyup yanına linki ekleriz indiririz
3.6	CPU-Only	https://cntk.ai/PythonWheel/CPU-Only/cntk-2.7.post1-cp36-cp36m-win_amd64.whl
GPU	https://cntk.ai/PythonWheel/GPU/cntk_gpu-2.7.post1-cp36-cp36m-win_amd64.whl

pip install cntk
conda install -c peterjc123 pytorch
conda install pytorch -c pytorch
conda install theano
pip install gym
conda install -c menpo ffmpeg
pip install nltk

import tensorflow as tf
import keras
import ntlk
import cntk
import torch

theano
conda install mkl-service libpython
conda install theano
gpu için
conda install theano pygpu

MKL_THREADING_LAYER=GNU hatası veriyorsa
set MKL_THREADING_LAYER=GNU deriz

Detayları modern deep learning kursunda.

import theano

x=T.scalar('x')

pip install gym

install openai gym windows  araştırdık
https://github.com/openai/gym/issues/11 sitesinde
27 Agustos alexlyzhov  kodlar
pip install gym
pip install git+https://github.com/Kojoley/atari-py.git
eğitimde nikkou kullanıcı adı.

conda install git çalıştırdık
pip install git+https://github.com/Kojoley/atari-py.git çalıştırdık

Hata alırsa buna da bakalım

edwarner commented on 22 Jun 2017
Hello,
Using the latest version of WinPython 3.5 (http://winpython.github.io/) on Windows 10, gym and atari installed drama-free using pip.
I ran the following two commands inside my python environment.

pip install gym
pip install git+https://github.com/Kojoley/atari-py.git

As a bonus, the latest versions of sk-learn, tensorflow, numpy, etc also work perfectly.
Hope this helps!

https://github.com/Kojoley/atari-py
github.com/Kojoley/atari-py/release açıp
atari_py-0.11-cp36-cp36m-win32-amd64.whl  indirdik

pip install atari_py-0.11-cp36-cp36m-win32-amd64.whl  deriz

sudo pip install tensorflow

Windowsta Theano ve TF desteklenmediğini söylüyor
En iyi yöntem vm içinde Linux kullanmak.

Macte
sudo pip install numpy scipy ipython pandas matplotlib theano

Xubuntu ya da Lubuntu kullanabiliriz bunlar light versiyonlar.
kurduktan sonra sudo apt-get update
sudo apt-get upgrade
sudo apt-get install build-essential
sudo ./VBoxLinuxAdditions.run 
sudo apt-get update
sudo apt-get install python-numpy python-scipy python-matplotlib 
ipython python-pip python-dev python-setuptools

sudo pip install --upgrade pandas theano

lokale de indirdim git repoyu
sudo apt-get install git
git clone git@github.com/lazyprogrammer/machine_learning_examples.git
cd machine_learning_examples/ann_class2
örnekleri çalıştıralım
python theano1.py
python tensorflow1.py

https://github.com/lazyprogrammer/machine_learning_examples/blob/master/numpy_class/classification_example.py
inceledik

https://deeplearningcourses.com/course_order#networkVewHeader

ML kısmını buraya yazdım

X 2D array N*D 
N örnek sayısı
D özellik sayısı
Y ise 1D arraydir N uzunlukta.
model=RandomForestClassifier()
model.fit(X,Y) learning içindir
model.predict(X) predictions içindir
model.score(X,Y) ile accuracy ölçebiliriz
correct/total = accuracy

terminalde ipython dedik açıldı sonra kodları çalıştırabiliriz
from sklearn.datasets import load_breast_cancer
classification_example.py kodları çalıştırdık
569 gözlem 30 feature özellik var bu datada

regression
predicting house prices
average family income, crime rate, size, number of bedrooms, 
when last renovated

Görselleştirirken
1 input ile line
2 input ile plane olur
2den fazla input varsa görselleştirilemez hyperplane olur
input özellik demek burada

Stock markette
düşük al, yüksekte sat
Input: 3 input mesela
son 10 günlük fiyatlar al, 
haberlerden bu durumdan bahsedildi mi? ve Positive miydi?
Predict yarının fiyatlarını tahmin et.
X 2d array N*D n number of samples, D number of features
    Feature1 Feature2 Feature3 ...  burası da D dir
N1
N2
.
.
N

Y 1d arraydir N uzunluğunda

Score olarak MSE alıyor mean square error
SUM((y-yhat)^2) / N

R2 için
R2 = 1 - (SSE/SST)  hata kareler toplamı / kareler toplamı
SSE = SUM((y-yhat)^2) 
SST = SUM((y-yort)^2) 

https://github.com/lazyprogrammer/machine_learning_examples/blob/master/numpy_class/regression_example.py
bu komutları uyguluyoruz

X_Train Y_Train ile fit edilir  model.fit(X_Train,Y_Train)
X_Train Y_Train ile scorea bakılır model.score(X_Train,Y_Train)
X_Test Y_Test ile scorea bakılır model.score(X_Test,Y_Test)
sonra X_test ile predict edilir model.predict(X_Test)

Feature Vector
ilk satır ilk gözlem     (10,3,0)
Her bir satır bir feature vectordür
Her bir kolon column bir feature özelliktir

Featurelar domain bilgisi ile ilk olarak belirlenebilir
Ama matematiksel yaklaşımla bakarsak x feature için X^2, X^3, X^4 de
featuredır

Raw datayı yükledikten sonra Deep neural network içinde
Automatic feature extaction yapar. Sonra classification yapar
En son da prediction

Codeler için genelleme
X, Y = load_date()
model = MakeModel()  model instance oluştur
model.fit(X,Y)       train model
model.score(X,Y)     evaluate model
model.predict(X)     use model

model=RandomForestClassifier()
model.fit(X,Y)
model.predict(X)

model=MLPClassifier()
model.fit(X,Y)
model.predict(X)

classification accuracy döner
regression R2 döner

Unsupervised algoritmalar label kullanmaz o yüzden Y yoktur

Ensemble Models: RandomForest, AdaBoost, ExtraTrees, Gradient Boosted Trees


